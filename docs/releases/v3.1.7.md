## 更新内容

- 启用 GitHub Actions 双站点发布：同一次构建将 OpsLog 和 OpsLog Reader 的程序包同步到 [Gitea 主站](https://git.alawn.cn/Alawn/opslog-release/releases) 与 [GitHub 备用站](https://github.com/AlawnCN/opslog-release/releases)。两个站点分别提供本站下载地址、校验和与自动更新清单。
- 修复 Gitea Release 上传完成后，对附件数量的校验误报。现在以 Gitea 实际返回的附件清单为准，检查文件是否齐全且无多余项。
- 桌面应用自动更新优先使用 Gitea，主站不可用时可回退到 GitHub；旧版客户端的原 GitHub Release 地址继续提供兼容更新清单。本次未改变日志查询功能。

## 发布包

- OpsLog 与 OpsLog Reader：Windows x64 安装版和绿色版；macOS Apple Silicon（ARM64）与 Intel（x64）的 DMG、绿色版和自动更新包。
- 下载后请使用所下载站点的 `SHA256SUMS` 校验文件完整性。

## macOS 首次启动

当前版本未使用 Apple Developer ID 签名或公证。如果 macOS 阻止启动，将应用拖到“应用程序”后，在终端执行：

```bash
xattr -dr com.apple.quarantine /Applications/OpsLog.app
```

OpsLog Reader 请执行：

```bash
xattr -dr com.apple.quarantine "/Applications/OpsLog Reader.app"
```

然后重新打开；如仍受阻，请在“系统设置 → 隐私与安全性”允许打开。移除隔离属性不等于 Apple 签名或公证。
